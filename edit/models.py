# -*- coding: utf-8 -8-
from django.db import models
from django.conf import settings


'''
# Create your models here.
class users(models.Model):
  name = models.CharField(u'Имя', max_length = 50)
  paycheck = models.IntegerField(u'Зарплата', default = 0)
  date_joined = models.DateField(u'Дата поступления на работу', default = '2014-01-01')

class rooms(models.Model):
  department = models.CharField(u'Отдел', max_length = 100)
  spots = models.IntegerField(u'Вместимость', default = 0)
'''




